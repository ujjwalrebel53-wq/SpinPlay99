# AlwaysData panel → Web > Sites → your site → Environment variables
# Copy IP and PORT from the site creation wizard (do not invent values).

IP=fd00::5:0000
PORT=8100

# Optional — same as .env (usually .env file is enough)
# WEB_ACCESS_PIN=1234
# UIDAI_PROXY=socks5://user:pass@indian-host:1080
# PLAYWRIGHT_BROWSERS_PATH=/home/ACCOUNT/.cache/ms-playwright
